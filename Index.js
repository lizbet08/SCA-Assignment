Const num1, num2;
Const num1 = window.prompt("Input the First integer", "0");
Const num2 = window.prompt("Input the second integer", "0");
                                                 
if(num1, 25) > (num2, 30) 
  { 
  document.write(`The larger of ${num1} and ${num2} is ${num1}`);
  }   
else
  if(num2, 30) > (num1, 20)
  {
  document.wtite(`The larger of ${num1} and ${num2} is ${num2}`);
 
  }                  
else
  {
   document.write(`The values of ${num1} and ${num2} are equal.`);
  }
  
